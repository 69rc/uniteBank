## Packages
recharts | For visualizing transaction data on the dashboard
lucide-react | Already in base stack, but emphasizing for iconography
date-fns | For formatting dates in transaction history tables

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
